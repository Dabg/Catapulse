name: Catapulse

default_view: TT

CatalystX::Inject:
  debug: 0
  path:
    - share/modules/
#    - __INC__
  inject:
    - CMS

Model::DBIC:
  traits:
    - FromMigration
  schema_class: Catapulse::Schema
  install_if_needed:
    default_fixture_sets:
      - all_tables


# - Database Model -
# Model::DBIC:
#  schema_class: Catapulse::Schema
#  connect_info:
#    dsn: dbi:mysql:database=catapulse;host=localhost
#    user: root
#    password: root
